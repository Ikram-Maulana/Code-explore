const element = (
  <div style={{
    width: 200,
    height: 200,
    backgroundColor: 'blue',
  }}>
  </div>
);

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(element);
